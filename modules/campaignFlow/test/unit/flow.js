'use strict';

/* jasmine specs for controller flow go here */

describe('controllers', function (){
  beforeEach(module('campaignFlow'));

  it('should ....', inject(function () {
    //spec body
  }));

  it('should ....', inject(function () {
    //spec body
  }));
});